{
  "private": true,
  "type": "module",
  "name": "thermostat-esp",
  "version": "1.0.15",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-prerender",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "preact-cli": "^3.4.1",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "@fseehawer/react-circular-slider": "^2.5.11",
    "htm": "^3.1.1",
    "luxon": "^3.0.3",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}
